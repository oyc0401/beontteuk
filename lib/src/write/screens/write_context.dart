import 'package:flutter/material.dart';

class WriteContext extends StatelessWidget {
  const WriteContext({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Container(),
    );
  }
}
