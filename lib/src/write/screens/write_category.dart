import 'package:flutter/material.dart';

class WriteCategory extends StatelessWidget {
  const WriteCategory({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Container(),
    );
  }
}
