import 'package:flutter/material.dart';

class WriteTitle extends StatelessWidget {
  const WriteTitle({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Container(),
    );
  }
}
